import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  root: {
    marginTop: 50,
  },
  rootGoOnline: {
    alignItems: 'center',
    borderRadius: 5,
    borderWidth: 1,
    padding: 10,
  },
});
